* Uncommon Machine

Reproducible scientific computation environment

provides emacs vim many hpc tools and high performace languages

um-run starts a system with persistence between sessions

* startup

git clone https://github.com/common-lamb/uncommon-machine.git ~/uncommon-machine

cd ~/uncommon-machine

cat commands.sh

source commands.sh

- commands
  - um-build 11
  - um-shell 11
  - um-push 11
  - um-pull
  - um-run

um- build/shell/push require an environment with podman and sudo permissions

um- pull/run require an environment with apptainer, but needs no sudo
